{
	"name": "blucket-blockstore",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"build": "cargo install --git https://github.com/CathalMullan/workers-rs worker-build && worker-build --release",
		"dev": "wrangler dev --local",
		"deploy": "wrangler publish"
	},
	"devDependencies": {
		"vitest": "^0.31.1",

		"wrangler": "^2.0.0"
	}
}
