{
	"name": "blucket-api",
	"version": "0.0.0",
	"description": "API and Authentication for Blucket",
	"type": "module",
	"main": "./src/index.js",
	"author": "Alex Miller <alex@banyan.computer>",
	"license": "Apache-2.0 OR MIT",
	"scripts": {
		"dev": "node scripts/cli.js import-firebase && wrangler dev --local --env dev",
		"deploy": "sh -c 'node scripts/cli.js import-firebase --env ${0} && node scripts/cli.js deploy --env ${0} --secrets'"
	},
	"dependencies": {
		"itty-router": "^2.6.1",
		"jose": "^4.14.4",
		"nanoid": "^3.1.30",
		"node-jose": "^2.2.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^3.14.1",
		"standard": "^17.0.0",
		"vitest": "^0.31.0"
	},
	"standard": {
		"ignore": [
			"dist"
		]
	},
	"engines": {
		"node": "18.x",
		"npm": ">=7.20.0",
		"yarn": "please-use-npm"
	}
}
