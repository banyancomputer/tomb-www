version: '3.8'
services:
  web:
    build:
      context: ./
      target: runner
    volumes:
      - .:/app
    command: npm run dev
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: development,
      NEXTAUTH_INTERNAL_URL: http://web:3000
    env_file:
      - .env.dev
